'use client'
// on button click.
// if I want to respond to a user input and run something?
export default function DeleteButton() {
    return (
        <button onClick={() => {}}>Delete</button>
    )
}