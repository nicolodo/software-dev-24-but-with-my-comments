console.log('Are you working...')

const myFavNumber = 45;

function countToTen() {
    sum(15,14)
    for (let i = 0; i <= 10; i++) {
        console.log(i)
    }
}

countToTen()



function sum(a,b) {
    // we can use the debugger statement to pause execution when we're in the sources tab
    debugger;
    return a + b
}